services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: whattsatende
      POSTGRES_PASSWORD: whattsatende
      POSTGRES_DB: whattsatende
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
